#include <stdio.h>
#include <stdlib.h>
#include "lista.h"

int main(int argc, char* argv[]){

No* H = no('A', no('B', no('C', no('E', no('F', NULL)))));

imprimir_lista(H);

printf("\n qtd ="%d")

    exit(0);
}